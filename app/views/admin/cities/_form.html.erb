<%= form_for(@admin_city) do |f| %>
  <% if @admin_city.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_city.errors.count, "error") %> prohibited this admin_city from being saved:</h2>

      <ul>
      <% @admin_city.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :city_name %><br>
    <%= f.text_field :city_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
