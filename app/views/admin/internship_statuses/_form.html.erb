<%= form_for [:admin,@internship_status] do |f| %>
  <% if @internship_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@internship_status.errors.count, "error") %> prohibited this from being saved:</h2>

      <ul>
      <% @internship_status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :internship_status_name%><br>
    <%= f.text_field :internship_status_name,required: true %>
  </div>
  <div class="actions">
    <%= f.submit 'Submit' %>
  </div>
<% end %>
